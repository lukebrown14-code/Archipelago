---
import BaseLayout from "./BaseLayout.astro";
const { title, description, category, date } = Astro.props;
---

<BaseLayout>
  <article class="mx-auto max-w-3xl py-10 md:py-14">
    <header class="mb-8 md:mb-12">
      <h1 class="text-3xl sm:text-4xl font-serif text-[var(--color-text)] mb-3 leading-tight">
        {title}
      </h1>

      {
        description && (
          <p class="text-xl md:text-2xl text-[var(--color-text)]/60 font-light max-w-2xl mx-auto md:mx-0">
            {description}
          </p>
        )
      }

      {
        (date || category) && (
          <div class="mt-4 flex items-center gap-3">
            {date && (
              <time class="text-sm text-[var(--color-text)]/50">
                {date.toLocaleDateString("en-US", { year: "numeric", month: "long", day: "numeric" })}
              </time>
            )}
            {category && (
              <span class="rounded-full bg-[var(--color-secondary)]/10 px-3 py-1 text-sm text-[var(--color-secondary)]">
                {category}
              </span>
            )}
          </div>
        )
      }

    </header>

    <div class="blog-content">
      <slot />
    </div>
  </article>
</BaseLayout>
